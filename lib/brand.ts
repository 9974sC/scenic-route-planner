export const APP_NAME = 'OKOLICA'

export const APP_TAGLINE = 'your neighborhood, by bike'

export const APP_TITLE = `${APP_NAME} — ${APP_TAGLINE}`

export const APP_DESCRIPTION =
  'Explore your neighborhood by bike — plan routes, claim the grid, and see every street you ride.'

/** Prefix for public user IDs (e.g. OKOLICA-AA0001). */
export const USER_ID_PREFIX = 'OKOLICA'

/** Legacy prefix kept for parsing existing accounts. */
export const LEGACY_USER_ID_PREFIX = 'SCENIC'

export const THEME_STORAGE_KEY = 'okolica-theme'

export const LEGACY_THEME_STORAGE_KEY = 'scenic-theme'

export const HTTP_USER_AGENT = 'okolica/1.0 (bike route planner)'
